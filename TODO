✔ Create choreography for reset state functionality @done(26-05-31 22:09)
✔ Implement emulation of github API in demo driver and add scenarios covering fetcher @done(26-06-01 11:15)
✔ Fetcher after each cycle of fetching must report event to control API containimg CI/CD api limits, own limits and own usage of ci/cd API, information about usage and limits must be shown on dashboard @done(26-06-05)
✔ Add views of control api and components event streams @done(26-05-31 22:40)
✔ Create adopter documentation and gh pages @done(26-06-01)
✔ Expose scalar UI via gateway @done(26-06-03)
✔ Add logo wherever its suitable @done(26-06-02 03:27)
✔ Make component events stream instead of polling @done(26-06-02)
✔ Optimize live poll with ETag/If-None-Match on /deployments and /deployments/{id}/statuses — a conditional 304 is free (does not count against the rate limit); GithubClient handles 304 but never sends If-None-Match, so F8 is effectively unimplemented. (Skip-terminal — not re-reading finished deployments — is DONE in #237; ETag would make the remaining per-cycle /deployments list + any in-flight deployment re-reads free too.) @done(26-06-03)
✔ Create release lifecycle for solution @done(26-06-01 22:27)
✔ Add trivy scanning for docker image build workflows @done(26-06-03)
✔ Fetcher live poll re-ingests OLD status events beyond BACKFILL_DEPTH. After a correct depth-2 backfill, the normal poll later re-posts deployment-status events from 2–3h ago, overfilling the history drawer with old in-progress/success rows. Suspect: post-backfill per-repo cursor `since` high-water mark not bounding live re-reads, or the terminal-skip/ETag (§5.5.1/§5.5.2) reused-deployment map resurfacing old deployments whose statuses then re-post. Expected: live poll ingests only status events newer than the cursor's per-repo `since`. Found during rate-limit feature live testing (separate from that feature). @done(26-06-05 23:24)
✔ Matrix view: let the user hide/show and reorder environment columns. Frontend-only — environments are currently server-sorted (Ordinal) and all rendered. Add a per-user column control (e.g. an environments picker mirroring the existing fields picker) to toggle column visibility and drag-reorder columns, with the preference persisted client-side (like the field-visibility toggles). No API/contract change; the matrix response still carries all environments — the view just filters/reorders them. [PR #259] @done(26-06-05 23:24)

# Open backlog migrated to GitHub issues:
#   #264 — emit a control event for every component control-API action
#   #265 — add a correlation ID to control/component events
#   #266 — split the gateway into a separate demo image
#   #267 — rework api-tests to the adopter full-pull path
#   #268 — distinct deployment statuses (pending/queued/waiting + cancelled/not-approved)
#   #269 — fetcher SSO-403 diagnostic
#   #270 — topbar component-reuse refactor
