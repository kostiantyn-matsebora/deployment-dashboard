{
  "name": "deployment-dashboard-mockup-visual",
  "private": true,
  "version": "0.0.0",
  "description": "Mockup-only Playwright harness — loads docs/deployment-dashboard.html in a real browser and asserts geometric visual invariants (no overlap, connectors reach targets, etc.). Owner: qa-engineer.",
  "scripts": {
    "test": "playwright test",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@playwright/test": "1.49.1",
    "@types/node": "22.10.2",
    "typescript": "5.6.3"
  }
}
