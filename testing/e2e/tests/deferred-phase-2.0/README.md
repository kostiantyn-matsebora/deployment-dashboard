# Deferred to Phase 2.0 — Playwright specs

Specs here are excluded from the active e2e run via the
`testIgnore: ['deferred-phase-2.0/**']` entry in
`testing/e2e/playwright.config.ts`. They are preserved (history via
`git mv`) so they can be reactivated when their feature returns to scope.

See `../scenarios/deferred-phase-2.0/README.md` for the canonical list
and the Phase 2.0 reactivation checklist.
