name: deployment-dashboard

# ─────────────────────────────────────────────────────────────────────────────
# Profiles
#
#   standalone  — Gateway + Frontend + API; connect to an external PostgreSQL instance.
#                 Set: API_KEY, POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_HOST
#
#   full        — Gateway + Frontend + API + managed PostgreSQL (Docker-volume-backed).
#                 Set: API_KEY, POSTGRES_USER, POSTGRES_PASSWORD
#
#   demo        — Gateway + Frontend + API + Demo Driver + managed PostgreSQL. Local evaluation only.
#                 Use docker-compose.demo.yaml override — no vars required.
#                 CORS: off (gateway mode)  |  Retention: 30 days
#
# Per-service profiles (gateway / frontend / api / postgres / demo-driver) allow
# a single service to be targeted in isolation, e.g. `--profile demo-driver`.
#
# The gateway is the single public surface (SAD §7, GW1).
# The frontend, API and PostgreSQL are internal-only; only the gateway exposes a port.
#
# Usage:
#   docker compose --profile standalone up
#   docker compose --profile full      up
#   docker compose -f docker-compose.yaml -f docker-compose.demo.yaml --profile demo up
# ─────────────────────────────────────────────────────────────────────────────

services:

  # ── Gateway (single public surface) ─────────────────────────────────────────
  gateway:
    image: ghcr.io/kostiantyn-matsebora/deployment-dashboard-gateway:latest
    pull_policy: always
    profiles: [gateway, standalone, full, demo]
    ports:
      - "${GATEWAY_PORT:-8080}:8080"
    environment:
      # Internal API upstream — fixed to the api service name in this network.
      API_UPSTREAM: "api:8080"
      # Internal frontend upstream.  The frontend image (nginx:alpine) listens
      # on :80.  Requests to / return 502 until the frontend container is
      # running; all /api/* and probe routes work without it.
      FRONTEND_UPSTREAM: "${FRONTEND_UPSTREAM:-frontend:80}"
      # Internal demo-driver upstream.  Optional — gateway starts cleanly when
      # the demo-driver container is absent; /demo/* returns 502 in that case.
      DEMO_DRIVER_UPSTREAM: "${DEMO_DRIVER_UPSTREAM:-demo-driver:3001}"
    depends_on:
      api:
        condition: service_healthy
      frontend:
        condition: service_healthy
        required: false   # frontend may be omitted (gateway serves /api/* alone)
      demo-driver:
        condition: service_healthy
        required: false   # demo-driver is opt-in (demo profile only)
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # ── Frontend (Angular SPA, nginx-served) ─────────────────────────────────────
  frontend:
    image: ghcr.io/kostiantyn-matsebora/deployment-dashboard-spa:latest
    pull_policy: always
    profiles: [frontend, standalone, full, demo]
    # Internal-only — the gateway proxies / to this service; no published port.
    restart: unless-stopped
    healthcheck:
      # nginx:alpine ships busybox wget (no curl); 200 on / means the SPA serves.
      test: ["CMD-SHELL", "wget -q -O /dev/null http://localhost:80/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # ── API ─────────────────────────────────────────────────────────────────────
  api:
    image: ghcr.io/kostiantyn-matsebora/deployment-dashboard-api:latest
    pull_policy: always
    profiles: [api, standalone, full, demo]
    environment:
      # Connection string assembled from individual parts so each can be
      # overridden independently.  Override POSTGRES_HOST for standalone.
      ConnectionStrings__Postgres: >-
        Host=${POSTGRES_HOST:-postgres};Port=${POSTGRES_PORT:-5432};Database=${POSTGRES_DB:-deployment_dashboard};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}
      # Write-endpoint shared secret.  Required — validated on app startup.
      API_KEY: "${API_KEY}"
      # Control-surface shared secret (API_SPECIFICATION.md D8).
      # Optional — absent (empty) hides POST /api/control/reset entirely (returns 404).
      # Set to enable the reset surface; keep distinct from API_KEY (least-privilege).
      CONTROL_API_KEY: "${CONTROL_API_KEY:-}"
      # CSV of allowed CORS origins.  Empty disables CORS (gateway / same-origin).
      # Default is empty — gateway mode is the standard deployment (GW6).
      # Set only when running the API without the gateway (split-domain mode,
      # API_SPECIFICATION.md D6); the two modes are mutually exclusive.
      CORS_ALLOWED_ORIGINS: "${CORS_ALLOWED_ORIGINS:-}"
      # Minimum 90, default 365.  Demo uses a shorter window.
      HISTORY_RETENTION_DAYS: "${HISTORY_RETENTION_DAYS:-30}"
    depends_on:
      postgres:
        condition: service_healthy
        required: false   # postgres is not started in the standalone profile
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/healthz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  # ── Demo Driver (opt-in, demo profile only) ──────────────────────────────────
  demo-driver:
    image: ghcr.io/kostiantyn-matsebora/deployment-dashboard-demo-driver:latest
    pull_policy: always
    profiles: [demo-driver, demo]
    environment:
      # Target Write API — must match the api service name in this network.
      WRITE_API_URL: "http://api:8080"
      # Must match the API_KEY configured on the api service.
      API_KEY: "${API_KEY}"
      # Must match CONTROL_API_KEY on the api service to allow POST /demo/api-reset.
      # Empty disables the API reset button in the control panel (returns non-2xx).
      CONTROL_API_KEY: "${CONTROL_API_KEY:-}"
      # Scenario JSON files are baked into the image at /app/data (Dockerfile).
      # Override to mount a custom scenarios directory.
      SCENARIOS_DIR: /app/data
      # Per-event delay for ingest runs (ms); 0 = bulk load.
      EMIT_DELAY_MS: "${EMIT_DELAY_MS:-0}"
      # Interval between periodic random events when Live Emission is enabled (ms).
      EMIT_INTERVAL_MS: "${EMIT_INTERVAL_MS:-8000}"
      PORT: "3001"
      # GitHub emulator base URL — used by the driver's /demo/health aggregator
      # to probe the emulator's /_github/status endpoint (DEMO_DRIVER_SPEC §9).
      GITHUB_EMULATOR_URL: "http://github-emulator:3100"
      # Fetcher host base URL — used by the driver's /demo/health aggregator
      # to probe the fetcher's /health endpoint (FETCHER_SPECIFICATION §3, §6).
      FETCHER_URL: "http://fetcher-host:8080"
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://localhost:3001/demo/status || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  # ── GitHub Emulator (opt-in, demo profile only) ──────────────────────────────
  github-emulator:
    build:
      context: ../demo
      dockerfile: github-emulator/Dockerfile
    profiles: [github-emulator, demo]
    # Internal-only — no gateway route; the fetcher reaches it directly.
    environment:
      PORT: "3100"
      GITHUB_SIM_RATE_LIMIT: "${GITHUB_SIM_RATE_LIMIT:-5000}"
      SEED_ON_STARTUP: "true"
      SCENARIOS_DIR: /app/data
      EMIT_INTERVAL_MS: "${EMIT_INTERVAL_MS:-8000}"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://localhost:3100/_github/status || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  # ── Fetcher Host (opt-in, demo profile only) ──────────────────────────────────
  fetcher-host:
    build:
      context: ../backend
      dockerfile: fetcher-host/Dashboard.Fetcher.Host/Dockerfile
    profiles: [fetcher-host, demo]
    # Internal-only — no published port, no gateway route (FETCHER_SPECIFICATION §3, §6).
    environment:
      DASHBOARD_API_BASE_URL: "http://api:8080"
      API_KEY: "${API_KEY}"
      CONTROL_API_KEY: "${CONTROL_API_KEY:-}"
      COMPONENT_ID: "dashboard-fetcher"
      POLL_INTERVAL_SECONDS: "${POLL_INTERVAL_SECONDS:-30}"
      GITHUB__BASE_URL: "http://github-emulator:3100"
      GITHUB__REPOS: "demo-org/payments-api,demo-org/search-indexer"
      GITHUB__TOKEN: "demo-token"
      GITHUB__VERSION_SOURCE: "artifact:version.txt"
      GITHUB__RATE_LIMIT: "${GITHUB_SIM_RATE_LIMIT:-5000}"
      GITHUB__RATE_LIMIT_BUDGET_PCT: "80"
      BACKFILL: "false"
    depends_on:
      api:
        condition: service_healthy
      github-emulator:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  # ── PostgreSQL ───────────────────────────────────────────────────────────────
  postgres:
    image: postgres:17-alpine
    profiles: [db, full, demo]
    environment:
      POSTGRES_DB:       "${POSTGRES_DB:-deployment_dashboard}"
      POSTGRES_USER:     "${POSTGRES_USER}"
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD}"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
