{{- if .Values.gateway.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "deployment-dashboard.componentName" (dict "root" . "component" "gateway") }}
  labels:
    {{- include "deployment-dashboard.componentLabels" (dict "root" . "component" "gateway") | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "deployment-dashboard.componentSelectorLabels" (dict "root" . "component" "gateway") | nindent 4 }}
  ports:
    - name: http
      port: 8080
      targetPort: http
{{- end }}
